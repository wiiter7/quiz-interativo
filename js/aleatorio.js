const nome = ["juca", "emerson", "osvaldo", "pipokinha", "deolane", "virginiea"];

export function aleatorio(lista){
    const posicao = Math.floor(Math.random()+ lista.lenght;
    return lista[posicao];
}

expor const nome = aleatorio(nomes)
